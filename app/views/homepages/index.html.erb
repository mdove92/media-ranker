
<main>
      <section class="spotlight">
    <h2 class="spotlight__header">
      <span class="spotlight__header--prefix">Media Spotlight</span>
      <%=link_to "#{Work.spotlight.title} by #{Work.spotlight.creator}", root_path, class:"spotlight__link-to" %>
               
    </h2>
    <p class="spotlight__description">
      <%= "#{Work.spotlight.votes.length} Votes" %>
    </p>
  </section>

<hr class="root__hr">

<section class="top-ten__container">

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Movies
  </h2>
  <% Work.by_category("movie").each do |movie|%>
  <ul class="list-group top-ten__list">
      <li class="list-group-item">
        <h4>
          <%=link_to "#{movie.title}"%>
          <small class="top-ten__creator">by<%="#{movie.creator}"%> </small>
        </h4>
        <p>     <%= "#{movie.votes.length} Votes" %></p>
      </li>
  </ul>
  <% end %>
  <!-- <a href="/works.movies">View More Movies</a> -->
</section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Books
  </h2>
    <% Work.by_category("book").each do |book|%>
  <ul class="list-group top-ten__list">
      <li class="list-group-item">
        <h4>
           <%=link_to "#{book.title}"%>
          <small class="top-ten__creator">by <%="#{book.creator}"%> </small>
        </h4>
        <p><%= "#{book.votes.length} Votes" %></p>
      </li>
  </ul>
  <% end %>
  <!-- <a href="/works.books">View More Books</a> -->
</section>

  <section class="top-ten__list-container">
  <h2 class="top-ten__header">
    Top Albums
  </h2>
   <% Work.by_category("album").each do |album|%>
  <ul class="list-group top-ten__list">
      <li class="list-group-item">
        <h4>
          <%=link_to "#{album.title}"%>
          <small class="top-ten__creator">by <%="#{album.creator}"%> </small>
        </h4>
        <p><%= "#{album.votes.length} Votes" %></p>
      </li>
  </ul>
  <% end %>
  <!-- <a href="/works.albums">View More Albums</a> -->
</section>

</section>

  </main>